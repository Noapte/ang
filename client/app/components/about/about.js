import angular from 'angular';
import aboutComponent from './about.component';

let aboutModule = angular.module('about', [

])

.config(($stateProvider) => {

})

.component('trRow', aboutComponent)
  
.name;

export default aboutModule;
